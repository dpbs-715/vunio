{
  "name": "dlib-directives",
  "version": "1.2.4",
  "description": "@DLib directives library",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.mjs",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "rimraf dist && rollup -c rollup.config.mjs",
    "clean": "rimraf .turbo node_modules dist",
    "runPublish": "pnpm publish"
  },
  "devDependencies": {
    "@DLib/typescript-config": "workspace:*",
    "@rollup/plugin-json": "catalog:build",
    "@rollup/plugin-terser": "catalog:build",
    "rollup": "catalog:build",
    "rollup-plugin-dts": "catalog:build",
    "rollup-plugin-esbuild": "catalog:build",
    "vue": "catalog:"
  },
  "peerDependencies": {
    "vue": "catalog:"
  },
  "dependencies": {
    "resize-observer-polyfill": "catalog:ui"
  }
}

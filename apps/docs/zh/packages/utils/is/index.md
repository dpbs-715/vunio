# 类型判断工具

## is

> 类型判断工具。

```ts
import { is, isObject, isArray, isFunction, isNumber, isString, isEmpty } from '@vunio/utils';
is(obj, type); //判断类型
isObject(obj); //判断对象
isArray(obj); //判断数组
isFunction(obj); //判断函数
isNumber(obj); //判断数字
isString(obj); //判断字符串
isEmpty(obj); //判断是否为空
```

export const enum CLI_ACTION {
  EXIT = '__exit__',
  BACK = '__back__',
}

export const enum TEXT {
  BYE = 'Goodbye! 👋',
  DEFAULT_SELECT_MESSAGE = 'Please select',
}

export const enum CMD_RUN_TYPE {
  RUN_HINT = 'hint',
}
